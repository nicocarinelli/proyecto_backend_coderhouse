<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{!-- Bootstrap Link --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{#if user}}/api/products{{else}}/api/session/login{{/if}}">
                    <img src="/logo.png" alt="Logo" width="50" height="40" class="d-inline-block align-text-middle">
                    NicCommerce
                </a>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" href="/api/products">Products</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/carts/{{ userCart }}/tickets">Tickets</a>
                        </li>
                    </ul>
                    {{#if user}}
                    <span class="navbar-text">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li>
                                <a class="nav-link" href="/api/session/current">Hola, {{ user.first_name }} {{ user.last_name}}.
                                (Perfil: {{ user.role }})</a>
                            </li>
                            <li>
                                <a class="nav-link" href="/api/session/logout">Logout</a>
                            </li>
                        </ul>
                    </span>
                    {{/if }}
                </div>
            </div>
        </nav>

        <div>
            {{{ body }}}
        </div>
    </div>

    {{!-- Bootstrap Script --}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
        crossorigin="anonymous"></script>
</body>

</html>